<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("../butcher/head.ejs") %>
  <title>Butcher Shop | <%= subpageName %></title>
</head>
<body>
  <noscript>Butcher Shop requires JavaScript. Please enable.</noscript>
  <header>
    <%- include("../butcher/banner.ejs") %>
  </header>
  <%- include("../butcher/nav.ejs") %>
  <main class="container-subpage">
    <h2 class="title"><%= pageTitle %></h2>
    <% if(product.coverImagePath) { %>
      <img
        width="300"
        height="300"
        src="<%= product.coverImagePath %>"
        alt="<%= product.name %>"
        title="<%= product.name %>"
      >
    <% } %>
    <h3 class="title">Description:</h3>
    <p><%= product.shortDescription %></p>
    <!-- Don't display Product story if Unavailable or Other -->
    <% if(product.story.name !== "Unavailable" && product.story.name !== "Other") { %>
      <h3 class="title">Story:</h3>
      <p><%= product.story.name %></p>
      <p><%= product.story.synopsis %></p>
      <p><%= product.story.introduction %></p>
      <p><%= product.story.content %></p>
    <% } %>
  </main>
  <footer>
    <%- include('../butcher/footer.ejs') %>
  </footer>
</body>
</html>