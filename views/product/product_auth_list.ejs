<h1><%= pageTitle %></h1>
<p>
  To test database connectivity, these products are retrieved from a mongoDb cloud
  database called E-Shop, a collection of documents called products which are linked to
  a collection of documents called stories.
</p>

<% products.forEach((product)=> { %>
<a style="padding: 16px 0" href="product/<%= product.url %>">
  <h2><%= product.name %></h2>
  <% if(product.coverImagePath) { %>
  <img
    width="800"
    height="600"
    src="<%= product.coverImagePath %>"
    alt="Database picture"
  />
  <% } %>
</a>
<% }); %> <% if (roles && roles.includes("Author")) { %>
<p><a href="/author/product/create">Create New Product</a></p>
<% } %>
